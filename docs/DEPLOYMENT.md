# Deployment

- Docker Compose: see `infrastructure/docker-compose.yaml`.
- Kubernetes: apply manifests in `infrastructure/k8s/`.
- Reverse proxy: `infrastructure/nginx/nginx.conf`.
- Monitoring: Prometheus config at `infrastructure/prometheus.yml`; add Grafana dashboards in `infrastructure/grafana/dashboards/`.

